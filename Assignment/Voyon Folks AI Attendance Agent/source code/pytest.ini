[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    llm: evaluation tests that call Azure OpenAI (use --run-llm)
    integration: full pipeline tests with mocked HRMS (use --run-integration)
    live_hrms: end-to-end tests against live HRMS (use --run-live-hrms)
